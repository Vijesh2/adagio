[project]
name = "adagio"
version = "0.1.0"
description = "FIFA 2026 World Cup prediction game"
requires-python = ">=3.11"
dependencies = [
    "beautifulsoup4>=4.12.0",
    "python-fasthtml>=0.12.30",
    "pytest>=8.0.0",
]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
